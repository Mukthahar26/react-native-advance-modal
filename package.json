{
  "name": "react-native-advance-modal",
  "version": "1.0.4",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist && tsc",
    "patchpublish": "npm version patch && npm publish",
    "watch": "tsc --watch & nodemon --watch dist --exec \"yalc push\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Mukthahar26/react-native-advance-modal.git"
  },
  "homepage": "https://github.com/Mukthahar26/react-native-advance-modal",
  "author": "Mukthahar Shaik",
  "license": "MIT",
  "description": "Advance customizable modal component for React Native.",
  "peerDependencies": {
    "react": ">=18",
    "react-native": ">=0.72"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-native": "^0.73.0",
    "nodemon": "^3.1.14",
    "typescript": "^5.7.2"
  }
}
